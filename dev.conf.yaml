- defaults:
    link:
      force: true
      create: true
      relink: true
    shell:
      stdout: true

- clean:
    ~/.config:
      recursive: true

- yay:
    - neovim
    - fzf
    - lua

- link:
    ~/.config/nvim: config/nvim