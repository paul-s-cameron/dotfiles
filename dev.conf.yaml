- defaults:
    link:
      force: true
      create: true
      relink: true
    shell:
      stdout: true

- clean:
    ~/.config:
      recursive: true

- yay:
    - fzf
    - lua
    - docker
    - mingw-w64-gcc

