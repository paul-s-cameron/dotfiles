- defaults:
    link:
      force: true
      create: true
      relink: true
    shell:
      stdout: true

- clean:
    ~/.config:
      recursive: true

- yay:
    - neovim
    - fzf
    - lua
    - docker
    - mingw-w64-gcc

- link:
    ~/.config/nvim/lua: config/nvim/lua
    ~/.config/nvim/init.lua: config/nvim/init.lua
