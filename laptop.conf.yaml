- defaults:
    link:
      force: true
      create: true
      relink: true
    shell:
      stdout: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
   ~/.config/hyprpanel/config.json: themes/hyprpanel/laptop.json

